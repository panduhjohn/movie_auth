<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Users</title>
</head>
<body>
    <h1>hey from all users</h1>
    <% users.forEach(user => { %>

        <% if(user) { %>
        <%= user.name %> </br>
    
        <% } %>

    <%  }) %>
</body>
</html>